---
description: tabby-uart-kit 项目规范（中文、版本、提交、目录结构）
alwaysApply: true
---

# tabby-uart-kit 项目规则

## 语言

- 与用户交流、文档说明、CHANGELOG、Git 提交说明：**使用中文**
- 代码标识符、npm 包名、Tabby API、技术术语可保留英文

## 版本号（SemVer）

- 格式：`MAJOR.MINOR.PATCH`（如 `1.0.0`）
- **一个版本对应一次 Git 提交**：仅在准备发布/合并时 bump 版本，日常小改动不要每个 commit 都改版本号
- 发布时同步更新（同一 commit 内完成）：
  - `package.json` 的 `version`
  - `CHANGELOG.md`（中文，按版本倒序；该版本下合并 Added/Changed/Fixed，不要拆成多个小版本条目）
  - `README.md` 中的版本号（如有）
- 建议约定：
  - `MAJOR`：不兼容的配置或行为变更
  - `MINOR`：向后兼容的新功能
  - `PATCH`：向后兼容的问题修复
- 开发过程中可积累多个改动，**合并为一条 CHANGELOG + 一个版本号** 再提交

## Git 提交

- 本仓库作者：`jack <jaeck_zj@163.com>`（仅 `git config --local`，不改全局）
- 提交说明用中文，推荐 Conventional Commits：
  - `feat:` 新功能（含版本发布，如 `feat: 发布 v1.0.1（…）`）
  - `fix:` 修复
  - `docs:` 文档
  - `chore:` 构建/工具链
  - `refactor:` 重构（不改变行为）
- **版本发布提交**：一个 commit 包含该版本全部代码变更 + `CHANGELOG` + 版本号 bump
- **非发布提交**：`docs:` / `chore:` / `refactor:` 等可不改版本；`fix:` / `feat:` 若未达发布节点也可先提交，版本号留到下次发布一并更新
- 示例：`feat: 发布 v1.0.1（中文化、i18n、高亮回刷）`

## 项目结构

```
src/
├── index.ts, config.ts, api.ts, settings*
├── shared/       # 通用工具（ansi、tab、color）
├── save-log/     # 日志保存
└── highlight/    # 关键字高亮（每窗口 8 槽，F8 / Shift+F8）
```

- 配置命名空间：`uartKit`（兼容旧 `saveOutput` 读取）
- 插件关键字：`tabby-plugin`；入口：`dist/index.js`

## 高亮产品约束

- 默认无高亮；仅用户选中文字 + F8 手动标记
- 每终端窗口独立 8 槽；Shift+F8 仅清除当前窗口
- 样式：**明亮底色 + 黑字**；8 组 light/dark 主题色
- 高亮只影响终端显示，不改变 strip 模式日志内容

## 构建

```powershell
npm install --legacy-peer-deps --ignore-scripts
npm run build
```
